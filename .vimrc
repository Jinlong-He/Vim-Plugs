if filereadable(expand("~/.vim/vimrc_common.vim"))
  source ~/.vim/vimrc_common.vim
endif

if filereadable(expand("~/.vim/vimrc_vundle.vim"))
  source ~/.vim/vimrc_vundle.vim
endif

if filereadable(expand("~/.vim/vimrc_nerdtree.vim"))
  source ~/.vim/vimrc_nerdtree.vim
endif

if filereadable(expand("~/.vim/vimrc_latex.vim"))
  source ~/.vim/vimrc_latex.vim
endif

if filereadable(expand("~/.vim/vimrc_ale.vim"))
  source ~/.vim/vimrc_ale.vim
endif

if filereadable(expand("~/.vim/vimrc_asyncrun.vim"))
  source ~/.vim/vimrc_asyncrun.vim
endif

if filereadable(expand("~/.vim/vimrc_ycm.vim"))
  source ~/.vim/vimrc_ycm.vim
endif

if filereadable(expand("~/.vim/vimrc_gutentags.vim"))
  source ~/.vim/vimrc_gutentags.vim
endif

if filereadable(expand("~/.vim/vimrc_color.vim"))
  source ~/.vim/vimrc_color.vim
endif

if filereadable(expand("~/.vim/vimrc_md.vim"))
  source ~/.vim/vimrc_md.vim
endif

if filereadable(expand("~/.vim/vimrc_lldb.vim"))
  source ~/.vim/vimrc_lldb.vim
endif

if filereadable(expand("~/.vim/vimrc_tab.vim"))
  source ~/.vim/vimrc_tab.vim
endif

if filereadable(expand("~/.vim/vimrc_NerdCommenter.vim"))
  source ~/.vim/vimrc_NerdCommenter.vim
endif
